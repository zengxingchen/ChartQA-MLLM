python
import matplotlib.pyplot as plt
import numpy as np

# make data
np.random.seed(1)
x = 4 + np.random.normal(0, 1.5, 200)

# create histogram counts and bin edges
n, bins, patches = plt.hist(x, bins=8, linewidth=0.5, edgecolor="white")

for i in range(len(n)):

# Include the right edge of the last bin

print(f"